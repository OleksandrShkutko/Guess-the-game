import './spinner.css';
import * as Image from '../../images';

const Spinner = () => {
  return (
    <>
      <Image.Logo className='pulse-element' size={300} />
    </>
  );
};

export default Spinner;
